console.log(soma(3, 4)) //funciton declaration permite chamar a função antes dela estar declarada.

//function declaration
function soma(x, y) {
    return x + y
}

//function expression
const sub = function (x, y) {
    return x - y
}

//named function expression (pouco usada)
const mult = function mult(x, y) {
    return x * y
}